# 站一站 · 产品修改报告（基于资深 PM 诊断）

> 本文档基于前序「资深产品经理诊断」结论（建议项 R1–R15）整理而成，将评估建议转化为**可评审、可落地的修改方案**。
> **状态声明**：本报告仅为规划文档，**未对仓库内任何代码、功能或文档做实际修改**；所有修改项需经评审后分迭代实施。
> 优先级沿用 P0 / P1 / P2；工作量以「相对人日（pd）」估算，仅供排期参考。

---

## 0. 修改原则（约束条件）

1. **守住隐私资产**：维持「完全离线、无网络权限、无第三方 SDK」的核心定位；任何新增上报/同步默认**本地优先 + 显式授权 + 可关闭**。
2. **先度量、后增长**：在启动增长动作前先具备最小埋点能力（R2），避免盲改。
3. **单端先行、生态后接**：Android 单端先打磨闭环，再考虑 iOS / 穿戴 / 平台生态。
4. **不接广告**：健康场景与隐私叙事冲突，商业化走订阅 / B 端，不走广告。

---

## 1. 修改总览

| 编号 | 维度 | 修改项 | 优先级 | 工作量 | 关联诊断 |
|------|------|--------|--------|--------|----------|
| M1 | 功能 | 被动站立检测（验证"真站立"） | P0 | 5–8 pd | R1 |
| M2 | 技术/产品 | 最小埋点体系（激活/响应/留存） | P0 | 3–5 pd | R2 |
| M3 | 品牌/内容 | 定位收敛与叙事重写（README/商店文案） | P0 | 1–2 pd | R3 |
| M4 | UX | 首启引导式 Onboarding + 默认推荐配置 | P0 | 3–4 pd | R4 |
| M5 | 分发/构建 | Release 签名包 + R8 混淆 + 商店上架 | P1 | 3–5 pd | R5 |
| M6 | 功能 | 习惯机制（目标承诺/连续奖励/轻社交） | P1 | 5–8 pd | R6 |
| M7 | 功能 | 处方性建议（基于个人峰值时段推荐） | P1 | 2–3 pd | R7 |
| M8 | 商业 | B 端企业健康方案（账号+后台，远期） | P1 | 20+ pd | R8 |
| M9 | 生态 | 接入 Health Connect / 厂商健康 | P1 | 4–6 pd | R9 |
| M10 | UX | 智能免打扰自动分类场景 | P2 | 3–4 pd | R10 |
| M11 | 技术 | 可选端到端加密云同步 | P2 | 6–10 pd | R11 |
| M12 | 竞争 | 品牌体系 + ASO + 内容种草 | P2 | 2–4 pd | R12 |
| M13 | 技术 | 模块化 + Feature Flag + 测试门禁 | P2 | 4–6 pd | R13 |
| M14 | UX | 无障碍（大字/TalkBack/色盲）+ i18n | P2 | 3–5 pd | R14 |
| M15 | 功能 | 可穿戴延伸（Wear OS 通知/表盘） | P2 | 5–8 pd | R15 |

---

## 2. 详细修改方案

### M1 被动站立检测（P0）— 验证"真站立"
- **现状**：应用只能"提醒"，无法感知用户是否真的起身；步数分支此前已移除，统计真实性存疑。
- **目标**：以被动方式确认站立/活动，使打卡与统计可信，减少"假打卡"。
- **方案（模块级，不涉及代码行）**：
  - 新增 `sensor/` 或 `health/` 模块，优先使用 `STEP_DETECTOR` / `ACCELEROMETER` 在计时区间内累计活动步数；
  - 可选接入 **Health Connect** 读取步数（见 M9），作为交叉验证源；
  - 在 `TimerService` 计时循环中融合"活动量"信号；`HomeViewModel` 在用户点击「我站起来了」时与传感器信号比对，给出"已确认/未检测到活动"提示；
  - 新增权限 `ACTIVITY_RECOGNITION`（Android 10+），运行时申请，未授予则回退为纯手动打卡。
- **涉及文件/组件**：`service/TimerService.kt`、`ui/home/HomeViewModel.kt`、`data/CheckInRepository.kt`、新增 `health/` 模块、`AndroidManifest.xml`（权限）、`ReminderSettingsDataStore`（开关）。
- **验收标准**：开启后，用户在提醒点真实走动可被自动识别；未授权权限时功能安全降级，无崩溃。
- **注意**：传感器数据**仅本地处理**，不上传；与隐私原则一致。

### M2 最小埋点体系（P0）— 解除 PM 失明
- **现状**：无任何 Analytics，对激活率、提醒响应率、留存曲线完全失明。
- **目标**：建立可度量产品健康度的本地优先埋点。
- **方案**：
  - 新增 `analytics/` 模块，定义事件（首次启动、开始计时、提醒送达、提醒响应类型、打卡、设置完成）；
  - **默认本地事件日志**（Room 表 `AnalyticsEvent`），不联网；在「设置 → 隐私」提供「匿名使用统计（可选上报）」开关，默认关闭；
  - 若开启，走用户自托管/自建轻量上报端点或仅本地导出 CSV，绝不接入第三方统计 SDK；
  - `SettingsDataStore` 增加 `analyticsConsent`。
- **验收标准**：可在本地查看激活漏斗（启动→配置→首次计时→首次响应）；上报开关默认关闭且可撤销。
- **注意**：严禁引入 Firebase Analytics / 友盟等第三方 SDK，违背隐私定位。

### M3 定位收敛与叙事重写（P0）— 品牌楔点
- **现状**：叙事为通用"久坐提醒"，易被系统原生能力替代。
- **目标**：收缩到鲜明人群/场景楔点，提升认知效率。
- **方案**：
  - 输出定位陈述（建议候选：「中国打工人的久坐教练」/「程序员·设计师专属工位健康」/「企业员工健康（B 端）」）；
  - 同步更新 README「项目简介」、应用商店简介、启动页一句话主张；
  - 沉淀 `BRAND.md` 定位说明，供后续文案统一。
- **验收标准**：README 简介、商店文案、启动页主张三者一致且指向同一楔点。
- **注意**：属内容/品牌改动，需与 R5 商店上架文案协同。

### M4 首启引导 Onboarding（P0）— 降低冷启流失
- **现状**：首次启动无引导，依赖用户自行配置。
- **目标**：3 步内完成可用配置，给出推荐默认值。
- **方案**：
  - 在 `ui/splash/` 或新增 `ui/onboarding/` 增加引导流程：① 欢迎与价值主张 → ② 选择提醒间隔（默认 30 分钟）+ 工作时间段（默认 9:00–18:00）→ ③ 权限引导（通知 / 使用情况访问可选）；
  - `SettingsDataStore` 提供"是否已引导"标记，首次启动后写入；
  - 引导中提供"一键使用推荐配置"按钮。
- **验收标准**：新装用户无需离开 App 即可完成首次有效计时；跳过引导后仍有合理默认值。
- **注意**：引导步骤不得强制授予非必要权限（使用情况访问为可选）。

### M5 Release 构建与上架（P1）— 打开获客
- **现状**：仅 Debug 签名 APK，`isMinifyEnabled=false`，无法上架。
- **目标**：产出可分发的 Release 包并上架至少 1 个商店。
- **方案**：
  - `app/build.gradle.kts` 的 `release` 设 `isMinifyEnabled = true`（启用 R8），完善 `proguard-rules.pro`；
  - 使用**自有签名密钥**（生成并安全保管 keystore，不入库），CI 走加密 Secret 签名；
  - 准备商店素材：图标、截图、隐私政策链接、功能描述（与 M3 一致）；
  - 评估上架渠道：Google Play / 华为应用市场 / F-Droid（开源友好）。
- **验收标准**：Release 包可安装、无崩溃、体积合理；成功在至少 1 个商店上架。
- **注意**：Debug 与 Release 签名不同，升级需卸载重装；密钥丢失将无法更新，务必备份。

### M6 习惯机制（P1）— 提升留存
- **现状**：仅有连续达标天数展示，无奖励/承诺/社交。
- **目标**：把"工具"变成"习惯养成"。
- **方案**：
  - `CheckInRepository` / `StatsViewModel` 增加目标设定（如"每小时站立 1 次"）；
  - `ui/stats/` 增加徽章/里程碑展示（连续 7/30 天、累计 100 次等）；
  - 轻社交：可选"与好友 PK/晨间打卡"（需账号，见 M8/M11，远期）；
  - 提醒文案多样化（`ReminderCopywriter`）按时段/连胜给予正向反馈。
- **验收标准**：用户可设定目标并看到进度与奖励；无社交依赖时单机也可用。
- **注意**：先做单机奖励，社交放到账号体系就绪后。

### M7 处方性建议（P1）— 从"描述"到"处方"
- **现状**：统计仅展示历史，不给出行动建议。
- **目标**：基于个人数据给出可执行的下一步。
- **方案**：
  - `StatsViewModel` 计算个人久坐高峰时段（如 14–16 点最集中）；
  - 在统计页/设置页给出建议：「建议将提醒间隔在工作时段调小至 X 分钟」；
  - 与 M2 埋点结合，建议随数据动态更新。
- **验收标准**：统计页出现至少 1 条基于真实数据的个性化建议，且建议可被一键采纳。
- **注意**：建议须可解释、可关闭，避免"算法黑箱"感。

### M8 B 端企业健康方案（P1，远期）— 商业化主路径
- **现状**：纯 C 端单机，无账号无后台。
- **目标**：面向企业售卖员工久坐健康方案，打开付费 TAM。
- **方案（范围级，非本期实现）**：
  - 新增账号体系（可选端到端加密，见 M11）+ 轻量后台（企业管理后台、团队达标看板）；
  - 企业版功能：部门达标排行、健康周报、管理员配置提醒策略；
  - 与 M5 上架、M11 同步协同设计。
- **验收标准**：企业可创建组织、邀请成员、查看匿名聚合看板。
- **注意**：工作量最大，需独立立项；隐私合规要求更高（企业数据脱敏）。

### M9 接入健康生态（P1）— 数据护城河
- **现状**：与健康平台零集成。
- **目标**：接入 Health Connect / 厂商健康，丰富数据并增强壁垒。
- **方案**：
  - 新增 `health/` 模块，声明 `Health Connect` 权限，读取步数/活动，写入站立记录；
  - 与 M1 共用传感器层；提供开关，默认关闭，显式授权；
  - 评估对接华为/小米运动健康（厂商 SDK）。
- **验收标准**：可在 Health Connect 中看到本 App 写入的站立记录；未授权不影响核心功能。
- **注意**：仅本地桥接，不新增网络出口。

### M10 智能免打扰自动分类（P2）
- **现状**：全屏免打扰依赖用户手动维护黑名单。
- **方案**：基于应用类别（视频/游戏/导航）自动识别前台全屏应用，减少手动配置；复用 `SmartDetector` + `UsageStatsManager`。
- **验收标准**：进入视频/游戏类应用时提醒自动延后，无需手动加黑名单。

### M11 可选端到端加密云同步（P2）
- **现状**：无账号无云同步，换机数据清零。
- **方案**：引入可选加密同步（如基于用户口令的 E2EE），同步 Room + DataStore；默认关闭。
- **验收标准**：开启后换机可恢复数据；关闭/遗忘口令则无法解密（符合 E2EE）。

### M12 品牌与分发运营（P2）
- **方案**：统一视觉/命名；商店 ASO 优化；在健康/职场社区做内容种草（图文/短视频）。
- **验收标准**：商店搜索"久坐提醒"等词可命中；有稳定自然新增。

### M13 工程化（P2）
- **方案**：模块拆分（`feature/`、`core/`）；引入 Feature Flag/远程配置（自建轻量）；CI 增加单元/UI 测试门禁与崩溃监控（自建或本地聚合）。
- **验收标准**：主流程有自动化测试覆盖；发版前 CI 拦截明显回归。

### M14 无障碍与本地化（P2）
- **方案**：支持系统大字模式、TalkBack、色盲友好配色；抽取字符串资源，预留 i18n。
- **验收标准**：在最大字体/ TalkBack 下主流程可用；文案集中于 `strings.xml`。

### M15 可穿戴延伸（P2）
- **方案**：新增 Wear OS 模块，推送手表通知/表盘并发症，复用提醒逻辑。
- **验收标准**：手表端可收到提醒并标记站立。

---

## 3. 分阶段路线图（建议）

- **Phase 1（夯实地基，约 12–19 pd）**：M1 被动检测 + M2 埋点 + M3 定位 + M4 Onboarding。目标：可信闭环 + 可度量 + 低流失。
- **Phase 2（可分发与差异化，约 17–27 pd）**：M5 上架 + M6 习惯 + M7 处方建议 + M9 健康生态。目标：能获客、有粘性、有壁垒。
- **Phase 3（规模化与商业化，远期）**：M8 B 端 + M10/M11/M12/M13/M14/M15。目标：多端、可变现、可运营。

---

## 4. 暂不修改 / 刻意保持项

- **保持完全离线、无网络权限**：隐私是核心信任资产，新增能力一律本地优先。
- **不引入广告与第三方统计 SDK**。
- **保持 Android 单端为当前主战场**，iOS/穿戴列入远期。
- **`PACKAGE_USAGE_STATS` 维持"可选、手动授权、未授权降级"**：不诱导授权。

---

## 5. 风险提示

- **M1/M9 涉及新权限**（ACTIVITY_RECOGNITION、Health Connect），需更新隐私政策与权限说明（同步 README 安全章节）。
- **M5 签名密钥**需安全备份，丢失即不可更新。
- **M8 B 端**触碰企业数据合规，须单独评估。
- 所有涉及权限/数据/网络的变更，落地时须同步更新 README 的「安全与隐私」章节与「必要权限一览」表。

---

*本报告为规划文档，未对代码库做任何实际修改。落地前请按 Phase 评审排期。*
